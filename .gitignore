# .NET
bin/
obj/
*.user
*.suo
.vs/
*.nupkg
*.snupkg
TestResults/
publish/
Logs/

# appsettings with credentials — dùng env vars hoặc secrets.json trên máy local
appsettings.Development.json
appsettings.Production.json
secrets.json
*.secrets.json
.env
.env.*
!.env.example
!.env.cloud.example

# Rider / JetBrains
.idea/

# Node / FE
node_modules/
dist/
dist-ssr/
*.local
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# Claude Code local settings
.claude/settings.local.json

# Local debugging artifacts - never commit
cookies.txt
prompt.txt

# GCP provisioning state & generated secrets - never commit
scripts/gcp/.secrets/

# Database backup artifacts produced by scripts/backup-database.sh - never commit
FoodSafe.BE/backups/

# ABP static libs restored at build time by `abp install-libs` — not source.
FoodSafe.BE/src/FoodSafe.HttpApi.Host/wwwroot/libs/

# Test-run artifacts (Playwright bookkeeping, visual-probe outputs) — never source.
# (testing/ui-audit/.gitignore already scopes its own screenshots/ output.)
test-results/
FoodSafe.FE/.results/
migration-backup/
